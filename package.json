{
  "name": "cf-docker-registry-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker Docker registry proxy with Docker Hub authenticated token exchange.",
  "type": "module",
  "scripts": {
    "test": "npx -y node@20 test/run-tests.js",
    "check": "npx -y node@20 test/run-tests.js"
  },
  "engines": {
    "node": ">=20"
  }
}
